import styled from "@emotion/styled";

const StyledLernplanSectionWrapper = styled.div`

`;

const LernplanSection = () => {
  return <StyledLernplanSectionWrapper></StyledLernplanSectionWrapper>
};

export default LernplanSection;
